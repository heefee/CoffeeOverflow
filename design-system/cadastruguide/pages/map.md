# Map Page Overrides — CadastruGuide

Overrides `MASTER.md` for `/harta`.

- Layout: map 70% left, property panel 30% right (desktop); bottom sheet on mobile.
- Panel background: white with `--shadow-lg`, not full teal background.
- Map controls: minimal; primary actions in panel.
- Status badges use semantic colors (valid=teal, expired=amber, missing=slate).
- Disclaimer banner: fixed bottom-left on map, max-width 420px, readable contrast.
